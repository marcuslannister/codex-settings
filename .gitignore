# macOS
.DS_Store

# Editor directories
.vscode/
.idea/

# Temporary files
*.tmp
*.swp
*~

# Logs
*.log

# Environment-specific
.env
.env.local

# Codex files
.codex-global-state.json*
history.jsonl
sessions
version.json
auth.json
installation_id
.personality_migration
models_cache.json
config.toml
config.toml.*
litellm_config.yaml.*
cache/
log/
node_repl/
shell_snapshots/
state_*.sqlite*
logs_*.sqlite*
goals_*.sqlite*
memories_*.sqlite*
session_index.jsonl
external_agent_session_imports.json
ambient-suggestions/
chrome-native-hosts-v2.json
computer-use/
hooks.json
process_manager/
rules/
sqlite/
vendor_imports/

# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class
.DS_Store

# Dependency directories
node_modules/
jspm_packages/

# Temp files
tmp
.research

# Claude local runtime settings
.claude/settings.local.json

# Codex runtime-managed plugin cache (241MB app-server binary)
plugins/

# Codex runtime-managed skills dir (.system bundled skills); the old tracked
# skills symlink is gone — Codex reads only ~/.agents/skills (agent-scripts ADR 0001)
skills/

# Codex queue and thread runtime state
.sandbox_migration
queue_*.sqlite*
thread_history_*.sqlite*
thread-writer-locks/
